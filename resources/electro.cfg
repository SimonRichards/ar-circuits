markerDir = "markers/";
modelDir = "models/";

proximityThreshold = 5000.0;
proximityDelay = 1;

camWidth = 800;
camHeight = 600;

book = {
	circuits = (
		{
			//type = "SimpleCircuit";
			resistors = {
                @include "model_resistor.cfg"
                markerProto = "acSupply-X.patt"
                values = [];
                number = 1;
			};
            capacitors = {
                @include "model_capacitor.cfg"
                markerProto = "diode-X.patt"
                values = [];
                number = 1;
            };
		}
	);
};
