markerDir = "markers/";
modelDir = "models/";

proximityThresh = 1000;
proximityDelay = 1;

camWidth = 800;
camHeight = 600;

book = {
	circuits = (
		{
			//type = "SimpleCircuit";
			resistors = {
                @include "model_resistor.cfg"
                markerProto = "acSupply.patt"
                values = [];
                number = 1;
			};
            capacitors = {
                @include "model_capacitor.cfg"
                markerProto = "diode-0.patt"
                values = [];
                number = 1;
            };
		}
	);
};
