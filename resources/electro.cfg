markerDir = "markers/";
modelDir = "models/";

camWidth = 800;
camHeight = 600;

book = {
	pages = (
		{
			//type = "SimpleCircuit";
			scenes = (
				{
					@include "marker_acSupply.cfg"
					@include "model_acSupply.cfg"
				},
				{
					@include "marker_diode.cfg"
					@include "model_diode.cfg"
				}
			);
		}
	);
};
