markerDir = "markers/";
modelDir = "models/";

camWidth = 800;
camHeight = 600;

book = {
	circuits = (
		{
			//type = "SimpleCircuit";
			resistors = {
                @include "model_resistor.cfg"
                markerProto = "resistor-XX.patt"
                number = 3;
			};
            capacitors = {
                @include "model_capacitor.cfg"
                markerProto = "capacitor-XX.patt"
                number = 2;
            };
		}
	);
};
